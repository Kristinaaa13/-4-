# 7) Найдовші дійсні дужки
# Стек індексів із базовим -1
def longestValidParentheses(s: str) -> int:
    st = [-1]
    best = 0
    for i, ch in enumerate(s):
        if ch == '(':
            st.append(i)
        else:
            st.pop()
            if not st:
                st.append(i)   # нова база
            else:
                best = max(best, i - st[-1])
    return best
# Складність: O(n) часу, O(n) пам’яті (у гіршому випадку).
